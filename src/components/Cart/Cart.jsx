import React from 'react';
import Counter from '../Counter/Counter';

const Cart = () => {
    return (
            <span>Carro
            <Counter />
            </span>
    );
};

export default Cart;
